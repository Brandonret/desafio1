"use client";
import React from 'react';

const Header = ({ count }) => {
    return (
        <header>
            <h1>Inscripción de Materias UDB</h1>
        </header>
    );
};

export default Header;
